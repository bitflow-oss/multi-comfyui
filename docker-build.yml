services:
  comfyui-build:
    build:
      context: .
      dockerfile: Dockerfile
    image: multi-comfyui:local
    environment:
      - CLI_ARGS=
      - HF_TOKEN=hf_abcdefg
      - LOW_VRAM=false
